---
description: Compose 增加 agent 服务、环境变量占位、密钥不进 Git、P2 不改表
globs: deploy/**,database/**,**/.env.example,**/.gitignore,**/Dockerfile,README.md
alwaysApply: false
---

# Compose / 密钥 / 表结构

- P2 **不改** `database/sql/schema.sql`（`task_type` 已是 `VARCHAR(32)`）。不加 `task_step` / `ai_call_log`。
- Compose 增加 `agent`：build context `../agent`，`container_name: async-forge-agent`，端口 `${AGENT_PORT:-8081}:8081`，网络 `async-forge`，healthcheck `GET /health`。
- backend 注入 `AGENT_BASE_URL=http://agent:8081`，`depends_on` agent healthy。仅本地只起 Java、Agent 在宿主机时才用 `localhost:8081`。
- `.env.example` 只放占位：`AI_BASE_URL`、`AI_API_KEY`、`AI_MODEL`、`AGENT_BASE_URL`、`AGENT_TIMEOUT_SECONDS`、`AGENT_PORT`。不要把 `deploy/.env` 的真实 Key 写进示例。
- gitignore 增加 `agent/.env`。镜像不烘焙 Key。Agent 镜像 Python 3.12-slim；uvicorn 听 `0.0.0.0:8081`。
- README 的启动、环境变量、三条演示、A2A method / 端口须与 skill 一致，禁止两套说法。
